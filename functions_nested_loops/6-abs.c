#include "main.h"

/**
 * _abs - check for absolute value
 * Descriptio: 'check if c is an alphabetical character'
 * @r: number to check
 * Return: 0 if code ok
 */

int _abs(int r)
{
	if (r < 0)
	{
		r = -r;
		return (r);
	}
	else
	{
		return (r);
	}
	return (0);

}
