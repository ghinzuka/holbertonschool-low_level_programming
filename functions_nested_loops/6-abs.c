#include "main.h"

/**
 * _abs - check for absolute value
 * Description: 'check if c is an alphabetical character'
 * @r : number to check
 * always return 0
 */

int _abs(int r)
{
	if (r < 0)
	{
		r = -r;
		return (r);
	}
	else
	{
		return (r);
	}
	return (0);

}
