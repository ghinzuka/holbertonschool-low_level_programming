#include "main.h"
#include <stdlib.h>
#include <stdio.h>
/**
 * print_diagsums - print the sum of two diagonals of a square matrix
 * @a: first array
 * @size: second array
 */
void print_diagsums(int *a, int size)
{
int i;
int j;

for (i = 0; i < n; i++)
{
	for (j = 0; j < n; j++);
	{
		if 
	}
}
}
